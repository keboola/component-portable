{
  "type": "object",
  "title": "Configuration Parameters",
  "required": [
  ],
  "properties": {
    "flow_id": {
      "type": "string",
      "title": "Flow ID",
      "propertyOrder": 30,
      "enum": [],
      "format": "select",
      "options": {
        "async": {
          "label": "Re-load flows",
          "action": "list_flows",
          "autoload": []
        }
      },
      "uniqueItems": true
    },
    "wait_until_finished": {
      "type": "boolean",
      "format": "checkbox",
      "title": "Wait for flow run to finish.",
      "default": false,
      "propertyOrder": 40
    },
    "fail_on_error": {
      "type": "boolean",
      "format": "checkbox",
      "title": "Fail job when Flow return error.",
      "default": false,
      "propertyOrder": 45,
      "description": "When checked, the Keboola job will fail when the flow ends with an error status.",
      "options": {
        "dependencies": {
          "waitUntilFinish": true
        }
      }
    }
  }
}